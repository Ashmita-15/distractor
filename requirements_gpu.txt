# GPU training dependencies only (Colab/Kaggle).
# torch is intentionally NOT listed: Colab/Kaggle ship a CUDA-enabled torch;
# reinstalling it usually downgrades to a CPU wheel or breaks CUDA.
# Pinned to the exact version Experiment 1 trained with, so trajectory
# comparisons across experiments are valid. NOTE: models saved by 5.6.0 do
# not load under the local 3.3.1 env — checkpoint evaluation runs on Colab.
# This file now covers the full Colab workflow (preprocess -> train ->
# evaluate), not just training, because Experiment 2 runs end-to-end on Colab.
sentence-transformers==5.6.0
datasets
accelerate>=0.26.0
pandas
numpy
scikit-learn        # Stage 01 split (train_test_split)
# Checkpoint evaluation (scripts/06) needs plotting + table rendering.
# faiss is intentionally omitted: retrieval falls back to an exact NumPy
# backend that is numerically identical to faiss.IndexFlatIP at this scale.
matplotlib
tabulate
